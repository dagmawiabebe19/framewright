import { SectionPlaceholder } from "@/components/layout/SectionPlaceholder";

export default function DeliverablesPage() {
  return (
    <div className="px-4 py-8 md:px-8">
      <SectionPlaceholder
        title="Deliverables"
        description="Cross-episode list of every turnover package with status, version, and file references."
      />
    </div>
  );
}
