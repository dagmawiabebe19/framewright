import { SectionPlaceholder } from "@/components/layout/SectionPlaceholder";

export default function AdrPage() {
  return (
    <div className="px-4 py-8 md:px-8">
      <SectionPlaceholder
        title="ADR tracker"
        description="Session-ready lists with recorded / approved states synced for the whole room."
      />
    </div>
  );
}
